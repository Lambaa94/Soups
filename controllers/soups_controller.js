const { Router } = require("express")
const express = require("express")
const model = require("../models/soups")

//create the router for the app
app.get()

app.post()

app.put()

app.delete()

module.exports = router;